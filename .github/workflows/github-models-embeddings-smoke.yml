name: GitHub Models Embeddings Smoke

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/github-models-embeddings-smoke.yml"
      - "scripts/github_models_embeddings_smoke.py"

permissions:
  contents: read
  models: read

jobs:
  prove-embeddings:
    name: Prove GitHub Models embeddings
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run embeddings smoke
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/github_models_embeddings_smoke.py
